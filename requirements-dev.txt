# Entorno unico para scripts, evaluacion y servicios.
# Instalar con: .\.venv\Scripts\python.exe -m pip install -r requirements-dev.txt

# Dominio y pruebas
pytest>=8.0
ruff>=0.6

# Vision y evaluacion
opencv-python-headless>=4.9
pillow>=10.0
numpy>=1.26
supervision>=0.25
fast-alpr[onnx]>=0.1
fast-plate-ocr[onnx]>=0.3

# Agente del borde
paho-mqtt>=2.0

# API de ingesta
fastapi>=0.115
uvicorn[standard]>=0.30
httpx>=0.27
pydantic>=2.7
