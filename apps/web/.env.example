# Copiar a .env.local. Ambas son publicas por diseno: viajan en el bundle del navegador.
# Lo que protege los datos es RLS, no el secreto de estas claves.
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_PUBLISHABLE_KEY=